# Where the Spring Boot backend lives. The dev server proxies /api/* to here.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Playwright smoke test (optional). When set, the admin smoke runs;
# otherwise it skips.
# LEGION_ADMIN_EMAIL=admin@gmail.com
# LEGION_ADMIN_PASSWORD=
